# Server port (Render sets PORT automatically)
PORT=8000

# Model file paths (relative to ml-service/ working directory)
MODEL_PATH=../ecg_risk_model.pkl
VITAL_MODEL_PATH=../vital_risk_model.pkl
MODEL_VERSION=1.0

# CORS — allowed origins for cross-service calls (comma-separated, or * for all)
ALLOWED_ORIGINS=https://smart-health-backend.onrender.com